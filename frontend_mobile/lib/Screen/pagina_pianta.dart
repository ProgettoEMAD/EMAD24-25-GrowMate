import 'package:flutter/material.dart';

class PaginaPianta extends StatelessWidget {
  const PaginaPianta({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}